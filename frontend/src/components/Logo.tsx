/** PropCore logo: emoji (house for property) */
export function Logo({ className = '' }: { className?: string }) {
  return (
    <span className={className} role="img" aria-label="PropCore">
      🏠
    </span>
  )
}
